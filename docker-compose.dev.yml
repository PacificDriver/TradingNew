# Режим разработки: монтируем исходники, изменения подхватываются без перезапуска.
# Запуск: docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build
# Реферальная программа: отдельный проект referral-site/ (http://localhost:3002)
# После добавления новых маршрутов: перезапустите frontend:
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml restart frontend

services:
  backend:
    command: npm run dev
    environment:
      REFERRAL_FRONTEND_ORIGIN: http://localhost:3002
    volumes:
      - ./backend/src:/app/src
      - ./backend/prisma:/app/prisma

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    command: ["npx", "next", "dev", "-H", "0.0.0.0"]
    environment:
      BACKEND_URL: http://backend:4000
      NEXT_PUBLIC_WS_URL: ws://localhost:4000
    volumes:
      - ./frontend/app:/app/app
      - ./frontend/components:/app/components
      - ./frontend/store:/app/store
      - ./frontend/lib:/app/lib
      - ./frontend/public:/app/public
      - ./frontend/middleware.ts:/app/middleware.ts
      - ./frontend/next.config.mjs:/app/next.config.mjs
      - ./frontend/tailwind.config.ts:/app/tailwind.config.ts
      - ./frontend/postcss.config.mjs:/app/postcss.config.mjs
      - ./frontend/tsconfig.json:/app/tsconfig.json
      - ./frontend/next-env.d.ts:/app/next-env.d.ts
      - ./frontend/package.json:/app/package.json
